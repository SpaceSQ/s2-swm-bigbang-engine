# ==========================================
# S2-SWM Big Bang Engine (V4.0.0) 核心依赖
# ==========================================

# 核心计算引擎 (零第三方依赖，纯 Python 原生)
# 仅依赖 built-ins: math, logging, time, uuid, json

# ==========================================
# 可选依赖 (用于 WebAudio 全息舱的数据桥接)
# ==========================================
websockets>=11.0.3   # 用于建立 14 维张量的前后端实时推送流
fastapi>=0.100.0     # (可选) 用于构建外部 API 神谕接口，干预物理场
uvicorn>=0.22.0      # (可选) FastAPI 运行环境